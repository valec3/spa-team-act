# Python dependencies for PDF extraction and paper tooling
# Install: pip install -r scripts/requirements.txt

# PDF text and table extraction
pdfplumber>=0.10.0
pypdf>=4.0.0

# Optional: advanced table extraction (requires OpenCV + Ghostscript)
# camelot-py[cv]>=0.11.0
